open Emotion;

global(
  "html",
  [
    fontFamily("'Arvo', serif"),
    fontSize(`px(28)),
    lineHeight(`em(1.15)),
  ],
);

global(
  ".leaflet-container",
  [height(`pct(100.)), fontFamily("'Arvo', serif")],
);

global("ul", [padding(`px(0)), listStyleType(`none)]);